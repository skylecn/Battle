using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum WindowPage
{
    Null = 0,
    MainWindow,

    /////////////////////////
    Length,
}

public enum EnforceWindowPage
{
    Null = 0,

    WarningWindow,
    LevelWindow,
    ClearWindow,
    WinWindow,
    LoseWindow,

    /////////////////////////
    Length,
}

